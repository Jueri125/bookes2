    <h2>User info</h2>
    <%= image_tag @user.get_profile_image(100,100) %>


   <table class="table">
     <thead>
      <tr>
          <th>name</th>
          <th><%= @user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= @user.introduction %></th>
        </tr>
      </thead>
    </table>

   <div class="row">
        <%= link_to edit_user_path(@user.id), class: "btn btn-outline-secondary btn-block edit_user.id" do %>
          <i class="fa-solid fa-user-gear"></i>
        <% end %>
      </p>
   </div>
    <h2 class="mt-3">New book</h2>
     <%= form_with model: @book, url: books_path, method: :post, class: "book-form", method: :post do |f| %>
    <div class ="form-group">
      <h4>title</h4>
       <%= f.text_field :title %>
    </div>

    <div class ="form-group">
      <h4>Opinion</h4>
      <%= f.text_area :body %>
    </div>


    <div class ="form-group">
       <%= f.submit 'Create Book', class: "btn btn-secondary", style: "background-color: green; color: white;" %>
    </div>
       <% end %>
